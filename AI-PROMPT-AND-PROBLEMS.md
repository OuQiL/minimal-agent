# AI Prompt 与问题解决记录

本项目是一个从零实现的 Agent Runtime。核心的循环控制、工具调用协议、会话管理、上下文管理全部自研，外部依赖只有三项，各自承担一项基础设施职责。

完整的 15 项技术决策与其依据记录在 `openspec\changes\build-minimal-agent-runtime` 的设计文档中，本文只挑关键环节说明。

***

## 一、工作流

### 第一阶段：plan mode定下最小可行方案

第一步需要决定整个项目要用什么，包括技术栈、协议等
这一步AI的职责不是产出代码，我们需要的是定下最小可行方案，给出不得触碰的红线
提示词如下
```
查看需求txt文档，翻译整个需求文案，总结为四大模块：
1. Loop实现
2. 工具实现
3. 对话隔离性实现
4. 上下文管理实现

```
### 第二阶段：用 OpenSpec 把方案规格化

方案定了之后，我使用OpenSpec把我无法明确表达的需求，翻译成ai能看懂的规格化方案
即openspec文件下MD格式设计文档
这一步主要是为了声明什么需要做什么不能做，不止是做一个ai能理解的翻译，也是一次重要的人工检查
实践中ai很容易去做最简单的实现而不是最符合需求的实现，这段必须人工验证

### 第三阶段：通过对话与直接修改，精进design文档

AI一开始给出的方案更多只是一个框架，有很多细节没有被考虑到，需要人工精进
比如工具调用，如果调用失败，则不只返回当前工具不存在，还要返回有什么工具可调用，防幻觉
这段承载了最多的对话
详情可见 **二、关键抉择**

### 第四阶段：落地与验证

通过AI+人工落地design文档
最后用真实模型、真实搜索、真实天气跑了一遍完整场景


## 二、关键抉择
::tip：这部分由AI辅助完成（我真不想一行行把提示词对话什么的都写下来），经人工审阅，对其负责
### 1. 依赖政策：只允许基础设施库

**背景**：需求要求「不能依赖现有 agent 框架完成主流程」，但没说 SDK 能不能用。

**我的判断**：把边界定在**职责**而不是**名称**上——凡属被考察能力（循环控制、工具协议、会话隔离、上下文管理）的一律自研；只承担传输、序列化、存储的库可以用。

**结果**：最终只有三项依赖，各自只做一件事。这条边界后来还挡住了几次「顺手引个库」的冲动，比如参数校验器最终是自研的约 80 行，因为内置工具只用得到 `string`/`number`/`required`/`enum`，为一个子集引入完整实现性价比为负。

### 2. 接入 OpenAI 兼容协议，而不是某家的原生接口

**背景**：需要真实 LLM API，但供应商未定。

**我的判断**：选兼容协议而非某家原生接口，换取供应商可切换——改地址与模型名即可换家，代码不动。

**结果**：开发中途从默认模型换到推理模型，只改了一行配置。同时也暴露出一个真实难点：推理模型的思维链放在 `reasoning_content` 这个**非标准字段**里，SDK 的类型定义和累加器都不保留它——「提取思考过程」这条需求的实际难点就在这里。

### 3. 循环用显式的有状态循环，不用递归

**背景**：循环需要轮次上限。

**我的判断**：轮次计数是天然的循环不变量，`for turn := range maxTurns` 直接表达上限语义。递归则要把计数沿调用栈传递，栈深度还会成为第二个隐性上限。

**结果**：循环的控制流一眼可读，上限保护实现为循环条件本身，不需要额外机制。

### 4. 流式输出是硬需求，它改变了两件事

**背景**：最初我以为可以用「一次性完整响应」简化收敛判断。

**我的判断**：流式是硬需求，而它推翻了那条简化路径，并带来一个无法回避的约束——**一旦开始吐字就不能透明重试**。重试要么导致内容重复，要么需要擦除已显示的内容，两者都劣于如实告知。

**结果**：重试边界被明确压缩到「首个 token 到达之前」，由 SDK 负责；流中断由应用层处理，保留已产出内容并如实说明。另外，分支判据从「响应里有没有工具调用」换成了 `finish_reason`——流式下前者是逐片到达的，在流结束前据此判断会出错。

### 5. 上下文管理：先把「什么不进 context」定下来

**背景**：需求专门追问「哪些信息要塞入 context 更合适」。

**我的判断**：与其罗列该放什么，不如先把**不该放的**划出来。最终明确三条：

- **思维链**（`reasoning_content`）落库供查看，但**不回填**后续请求——多轮间体积增长快，且推理模型并不要求把上一轮的思维链再喂回去
- **工具调用 trace** 只落库，那是给人看的，不是给模型看的
- **已被压缩的历史原文**只以摘要形式存在

**结果**：进 context 的内容收敛为三段式——系统提示（含数据边界声明与会话标识）→ 压缩摘要 → 最近若干轮原文。每一段的位置都有理由，比如摘要紧跟系统提示而不混进历史消息，是为了让它作为「背景知识」而非「对话内容」被理解。

### 6. 压缩模板：两段式，且用户原话不设上限

**背景**：上下文超长需要压缩，但摘要最容易丢的恰恰是用户说过的话。

**我的判断**：让模型先用 `<analysis>` 草稿按时间顺序把对话逐条梳理一遍，再输出 `<summary>` 正文。草稿的作用是**逼模型在动笔前完整过一遍原始对话**，落库前会被剥掉、不占上下文。

正文按固定七节组织，其中\*\*「用户消息原文」一节刻意不设字数上限\*\*（逐条列出用户说过的每一句话，一条都不漏），其余六节合计限制在 500 字以内——把预算让给最不该丢的信息。

**结果**：真实模型完全按要求输出，用户原话一条不漏，工具结论也被准确保留。解析上做了四级降级（取 `<summary>` 块 → 缺闭合标签则取到结尾 → 只输出草稿则取其后的内容 → 完全不合格式则整段剥标签保留），宁可存下不那么规整的摘要，也不要退回纯规则裁剪、把这段历史彻底丢掉。

### 7. 工具并发

**背景**：最初评估「同一轮多工具并发执行」时，AI的判断是**不要做**——当时的工具是本地微秒级操作，并发收益接近零，而 SQLite 单写者要加锁，成本是实打实的。

**我的判断**：一次对外搜索是200ms左右的网络往返，模型若在同一轮请求三个独立查询，串行就是三倍等待。于是改为并发执行。

**结果**：加了三条不变式——结果按**调用序号**定序写入预分配切片（与完成先后无关，否则上下文顺序会抖动、测试不可复现）；单个失败不取消同批其他调用；goroutine 内只做网络调用与解析，写库统一在并发结束后串行进行。

这条值得单独记下来：**结论依赖前提，前提变了结论就该变**。前提写在设计文档里，所以将来有人问「为什么不一早就并发」时有据可查。

### 8. 接真实外部服务时，可测试性怎么保

**背景**：工具从「本地 mock」变成「真实网络调用」后，最大的风险是测试从此要么联网、要么覆盖不到解析逻辑。

**我的判断**：**注入传输层，而不是替换工具输出**。两个工具都把 API 端点与 HTTP 客户端作为构造参数，测试传入本地模拟服务的地址。

这个区别很关键：如果只把工具的输出做成可替换，测试就绕过了真实的解析路径——而字段名、嵌套层级、编码、空值恰恰是最易错的部分。注入传输层后，测试跑的是**完整的真实解析代码**，只是数据来自可控服务器。

**结果**：「测试不依赖网络」与「解析逻辑高覆盖」这两个目标不再冲突。同时给真实服务留了一组按凭据门控的集成测试，专门覆盖模拟服务覆盖不到的部分——**真实模型能否理解我提交的工具 Schema**。因为 Schema 写得不合模型口味时，工具永远不会被调用，而模拟服务会忠实按脚本调用，测试照样全绿。

### 9. 会话 UX：随机标识不适合手输

**背景**：会话标识是随机串（形如 `sess_2efdb9c24d`），列表里也看不出每段对话是什么。

**我的判断**：单用户场景下对外只暴露**递增编号**，并且编号必须按创建顺序分配、**不随列表排序变化**——列表按最近活动倒序排，位置会变，编号不会，否则用户记下的编号隔天就指错对象了。列表再带上**首条用户消息的片段**用于辨认。

顺带做了一个决定：**启动时不预建会话**。原行为是启动即建一个，结果是每次启动都留下一个空会话，在列表里越攒越多；改为首次发消息时才惰性创建。

**结果**：会话管理变成 `[1]` `[2]` 这样可以直接手输的编号，`/list` 一眼能认出每段对话。内部标识仍然可用（脚本与日志需要精确定位），但不出现在任何面向用户的文案里。

### 10. 写给模型的文本，必须是模型能理解的

**背景**：压缩历史时，工具结果那一行原本渲染的是**调用标识**（形如 `call_00_j4ppBskuLmdE734MytOH6512`）。

**我的判断**：那是协议层的机器标识，对模型而言只是一串随机字符，无法据此判断结果来自哪个工具。真实模型因此写下了含糊结论：

> todo 记录：助手确认已记录待办；**未展示具体工具调用**。

改成工具名之后，同一条摘要变成：

> todo（参数 action=add，content=买牛奶）：返回已记录待办（编号 1）：买牛奶。

**结果**：歧义消失。实现上有个容易忽略的点——工具名不在工具结果消息上，它在前一条助手消息的调用记录里，而那条消息一定先出现，所以边遍历边建立映射即可；映射缺失时如实说明「未能确定是哪一个」，既不编造也不退回塞标识。

**这条是普遍适用的**：内部标识（会话 ID、调用 ID）用于程序关联，面向模型的文本必须用模型的语言。

### 11. 配置三级装载

**背景**：密钥、模型、各项限额需要可配置。

**我的判断**：按「内置默认值 → 配置文件 → 环境变量」三级装载，后者覆盖前者。日常配置写在文件里便于查看与留档，临时切换模型或密钥用环境变量覆盖，不必改文件。

两个细节是刻意的：**未知字段直接报错**（把 `api_key` 打成 `api_kye` 会被明确指出，否则这类错误会表现为「明明配了却不生效」，极难排查）；**默认位置的文件不存在时静默回退**（不建配置文件的人不该被迫建一个），但**显式指定**的路径不存在则报错（那多半是路径写错了）。

**结果**：含密钥的配置文件已纳入版本库忽略，另附样例文件与一条「直接装载样例」的测试，防止样例与实际字段定义漂移。

***

## 三、问题定位与解决

开发过程中，我通过测试定位出了真实缺陷，有些是决策问题，由于设计文档终究无法面面俱到，所以需要根据实际情况调整
这里先列出这类问题
### 1. 工具调用在context中是一串ID而非名称
这是设计上的一个错误，应该用工具名而不是ID，否则对LLM来说没有意义，没有·语义，这是非常不好的行为

### 2.摘要功能欠缺考虑，直接给出300字上限
这块一开始design文档没有详细说明，我只在测试中发现，所以才调整了设计，直接给出300字上限直接导致记忆大量丢失不可避免，这是非常不好的行为，我做的改正很简单，仿造Claudecode的设计，将上下文压缩模块的提示词修改，让llm输出较为结构化的内容


还有的问题就是跟我们脑子里想象的不一样的东西，这块是单测与脚本中发现，由AI定位，我做的很简单，决策这是不是错误，以及怎么改
由于内容繁多，我只给出框架，由AI辅助生成，再由我终审修改，本人对以下内容负责
### 1. 压缩后出现「没有提问的回答」

**现象**：压缩测试断言「保留最近 3 轮 = 6 条消息」，实际得到 7 条。

**定位**：打印被保留的消息序列，发现里面有一条属于第一轮的助手回复。

**根因**：保留最近 N 轮时，切分点取在了「第 N+1 条用户消息**之后**」，于是把属于那一轮的助手回复与工具结果留了下来，在上下文里形成**没有对应提问的孤立回答**。

**修复**：切分点对齐到用户消息**本身**。

**教训**：修正后我的测试期望也一并订正了（原本写 3 条，正确应为 4 条）。**测试失败时，先确认期望本身是否正确**，否则会去修一个本来正确的实现。

### 2. 规格与设计互相矛盾

**现象**：压缩的第二个断言始终失败——「裁剪后仍超阈值」。

**定位**：对比两份产物发现矛盾——测试规格要求「压缩到阈值以内」，但设计只写了「保留最近 K 轮**原文**」。如果这 K 轮本身就超阈值（例如某轮含一大段搜索结果），两者无法同时成立。

**根因**：设计描述的是一个**触发条件**，规格却把它当成了一个**硬保证**。

**修复**：规格是行为契约，优先满足它。实现上补了硬上限——保留最近 K 轮之后若仍超阈值，就从最旧的一端**整轮**丢弃直到落回界内。唯一例外是最后一轮（即使它单独超阈值也保留，否则模型连当前的问题都看不到；这个例外不会失控，因为单条工具结果本身另有截断上限）。同时回填了设计文档。

**教训**：规格与设计冲突时，不要悄悄把规格改窄去迁就实现——那等于把缺陷写进文档。正确顺序是：确认哪一份是行为契约 → 满足它 → 回头修另一份。

### 3. 集成测试才暴露的覆盖缝隙：工具拿不到会话标识

**现象**：真实模型集成测试里，`todo` 工具报「无法确定当前会话」。

**根因**：循环从未把会话标识放进 context。**而这是单元测试的覆盖缝隙**——工具层测试自己构造了带会话的 context（所以通过），循环层测试又没用过 `todo` 工具（所以没发现）。两个测试各自成立，缝隙里漏掉了「谁负责把会话塞进去」这个问题。

**修复**：在循环调用工具执行器之前，把会话标识放进 context。并补了两条**离线**用例覆盖该缝隙，不再依赖真实模型才能发现。

**教训**：单元测试各自通过，不等于集成点是通的。**测试的边界画在哪里，决定了哪类缺陷能漏过去**——这也是后来专门加一组真实模型集成测试的直接动因。

### 4. 注册表被自己的别名卡住

**现象**：所有涉及工具注册的测试在装配阶段就失败——「工具 search 的别名 "websearch" 已被占用」。

**根因**：该工具声明了两个别名 `web_search` 与 `websearch`，而归一化（转小写、剥离下划线）之后两者是**同一个键**——工具和自己冲突。

**修复**：区分两种情况——同一工具内部归一化后重叠的别名无害，跳过即可；**跨工具**的别名冲突必须拒绝，否则模型请求的名称会指向错误的对象。

**教训**：归一化会压缩命名空间。凡是基于归一化键做唯一性校验的地方，都要考虑「同一个实体的多个表示形式碰撞」。

### 5. `-race` 在 Windows 上不可用

**现象**：任务清单里有两项要求「以 `-race` 运行不报告数据竞争」，实际执行时报错。

**根因**：Windows 上的竞态检测器依赖 cgo，而本机 `CGO_ENABLED=0` 且没有 C 编译器。

**处置**：这属于**验证方式本身不可用**，不是我该悄悄跳过的事。确认不安装 C 编译器后，改用两项不变量断言作为替代证据——「交错耗时下回填顺序恒等于调用序号」与「该结论在重复 20 次运行中稳定」；任务清单里的验证描述也同步更新，README 中如实说明这条约束。

**教训**：验证方式也会失效。这时应当**报告并调整**，而不是换个较弱的检查还不说明。

### 6. 断言的层次：Go 的 JSON 转义

**现象**：验证压缩提示模板时，直接对原始请求体做字符串匹配查找 `<analysis>`，失败了。

**根因**：Go 的 `encoding/json` **默认把** **`<`** **`>`** **`&`** **转义**。线上格式里没有字面量的尖括号，但服务端解析后模型拿到的就是 `<analysis>`——功能完全正常，是我的断言层次错了。

**修复**：断言改为针对**解码后的内容**。

**教训**：断言应当针对**含义**，而不是传输编码——否则测的是 JSON 库的行为，不是自己的逻辑。

***

## 四、几条做法

1. **先实测，再决策。** 涉及外部依赖、第三方库、网络服务的选型，先写探针跑一遍，不凭记忆下结论。SQLite 驱动只能选纯 Go 实现、某个天气 API 无需密钥、某个搜索服务不可达——这些都是实测出来的前提，不是推理出来的。
2. **让数据决定取舍。** 选 SDK 时，两个候选各有拥趸；实测发现官方 SDK 能正确拼装跨分片的工具调用参数，而另一个不能。这把选择从「偏好问题」变成了「用约 10 行机械代码换掉约 50 行高易错代码」，结论就清楚了。
3. **写下来的前提，才经得起前提变化。** 工具并发那条决策，最初判断是「不要做」；确认要接真实 API 后翻转成「要做」。前提写在设计文档里，所以这个翻转是有据可查的，而不是反复无常。
4. **区分「代码的问题」与「验证的问题」。** 这次开发中，有三次「故障」最后查明是验证手段本身造成的假象——测试工具的编码假设、验证脚本的路径假设、终端输出的显示假设。**先确认是哪一边的问题，再决定改什么**；否则会去修一个根本不存在的 bug，或者更糟：把一个本来正确的东西「修」坏。
5. **把限制如实写进文档。** 竞态检测用不了、搜索依赖单一服务、压缩是基础策略——这些都写在 README 的「已知限制」里。**说清楚没做什么，和说清楚做了什么同样重要。**

