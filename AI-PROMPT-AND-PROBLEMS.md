# AI Prompt 与问题解决记录

本文件记录开发过程中实际使用的协作方式，以及遇到的真实问题与定位过程。写这份记录的原则是**如实**：包括我判断错的地方，以及是什么抓住了那些错误。

---

## 一、关键的 Prompt 模式

### 1. 先实测，再决策

涉及外部依赖、第三方库、网络服务的选型，不凭记忆或推测下结论，先写探针实测。

> 「先探一下 Go 有没有装、CGO 是什么状态」
> 「写个探针跑跑看 modernc.org/sqlite 能不能拉下来、能不能建库读写」
> 「两个 SDK 的流式 API 都验一遍，特别是有没有办法把 reasoning_content 取出来」

这条模式带来的直接收益：在写第一行业务代码之前，就确定了 `mattn/go-sqlite3` 在本机不可用（无 C 编译器）、OpenWeatherMap 与 Brave Search 在本地网络下超时、Open-Meteo 无需密钥即可用。这些都是设计文档里无法靠推理得出的前提。

### 2. 让数据而不是说服力决定取舍

> 「用探针数据说明哪个更好，而不是『通常认为』」

SDK 选型时，两个候选各有拥趸。实测后发现官方 SDK 的流式累加器能把跨分片的工具调用参数正确拼装（`{"city":"北京"}`），而社区版没有累加器。这个事实把选择从「偏好问题」变成了「收益不对称问题」：用约 10 行机械代码换掉约 50 行高易错代码。

### 3. 区分「规格要求」与「设计描述」

> 「这条是规格里的行为契约，还是设计里的实现选择？两者冲突时以哪个为准？」

这个区分在第 5 个问题里起了决定性作用。没有它，我会把规格改窄以迁就实现——那是把 bug 写进文档。

### 4. 要求解释而不只是结论

> 「为什么不做模糊匹配？」
> 「这条为什么值得做？」

迫使每个取舍都有明确理由，而不是「大家都这么做」。例如「工具结果幂等复用」这条建议，追问后发现 `todo` 是有状态工具，缓存查询结果会在写入后返回过期数据——是正确性 bug 而非性能优化。**拒绝了缓存，但保留了它背后的诊断价值**（重复调用写进 trace 并告警）。

### 5. 对方案提出质疑

需求方给出一组「计策」（节点+channel+superstep、图级 state、branch 条件边等，均来自 LangGraph 模型），要求逐条判断。

> 「你可以自行判断哪些是需要的哪些是不必要的，告诉我」

结果 6 条中拒绝了 4 条，理由不是「简单点好」，而是它们在这个具体拓扑下无处施展：循环是 3 节点 1 回边的线性结构，superstep 调度器没有东西可调度。同时**采纳了其中两条**（`MessageRewriter` 钩子、名称别名），并**翻转了一条**——多工具并发最初被拒（工具是本地微秒级操作，并发是纯负债），在确认搜索与天气要接真实 API 后改为采纳（数百毫秒的网络往返，并发收益实打实）。

这个翻转是值得记录的：**结论依赖前提，前提变了结论就该变**。前提写在设计文档里，将来有人问「为什么不一早就并发」时，答案有据可查。

---

## 二、问题记录

### 问题 1：SQLite 驱动选型被本机环境直接排除一个选项

**现象**：Go 生态最常用的 SQLite 驱动是 `mattn/go-sqlite3`，需要提前确定能不能用。

**定位**：`go env` 显示 `CGO_ENABLED=0`；`gcc` / `cc` / `clang` / `cl` 全部不存在。

**根因**：`mattn/go-sqlite3` 是对 SQLite C 源码的 cgo 绑定，没有 C 编译器就无法编译。

**处置**：改用纯 Go 的 `modernc.org/sqlite`（SQLite 官方 C 源码到 Go 的机械翻译）。写探针实测：31 秒拉取、38 秒首次编译、建表/写入/按会话隔离查询全部正常（SQLite 3.53.4）。

**教训**：技术选型的第一步是确认环境允许什么，而不是确认哪个最流行。

---

### 问题 2：思维链提取失败——一个被测试抓住的隐蔽陷阱

**现象**：早期的探针明确取到了 `reasoning_content`。但封装成 `llm.Client` 后，单元测试断言思维链内容时拿到的是**空字符串**。

**定位过程**：

1. 先确认 fixture 产生的 SSE 原文——`reasoning_content` 确实在 delta 里，排除测试数据问题。
2. 再写一个内部调试测试，绕过自己的封装直接调用 SDK，打印每个分片的解析结果：

```
--- 分片 2 ---
  原始 JSON: {"choices":[{"delta":{"reasoning_content":"甲"},"index":0}],...}
  ExtraFields 数量=1
    reasoning_content -> valid=false raw="甲"
```

**根因**：`Field.Valid()` 返回 `false`，而我的代码写的是：

```go
if !ok || !f.Valid() {
    return ""
}
```

SDK 对所有「不在类型定义内的额外字段」一律报告 `Valid() == false`——它本意是「这个字段不在我的类型契约里」，而不是「这个值无效」。按 `Valid()` 过滤，等于把思维链整体丢掉。

**修复**：不以 `Valid()` 作判据，只检查字段是否存在、以及原始内容是否为空或 null。

**教训**：探针当初之所以成功，恰恰是因为它**只检查了 `ok` 而没检查 `Valid()`**。封装时我「顺手补上」了一个看起来更严谨的检查，反而引入了 bug。这提示：探针代码与生产代码之间的差异，本身就是需要被测试覆盖的地方——探针验证的是「可行」，不是「已正确实现」。

---

### 问题 3：压缩后保留了无对应提问的孤立回答

**现象**：压缩测试断言「保留最近 3 轮 = 6 条消息」，实际得到 7 条；且裁剪后仍超出阈值。

**定位**：打印被保留的消息序列，发现里面有一条属于第一轮的助手回复。

**根因**：`RecentTail` 在丢弃轮次时返回的是 `msgs[i+1:]`——从「第 turns+1 条用户消息**之后**」开始截取。这会把属于那一轮的助手回复与工具结果留下来，在上下文里形成「没有对应提问的孤立回答」。

**修复**：切分点对齐到用户消息**本身**（`msgs[i:]`）。

**教训**：这一条修正同时暴露了我的测试期望也写错了（原本写 3 条，正确应为 4 条）。**测试失败时，先确认期望本身是否正确**，否则会去修一个本来正确的实现。

---

### 问题 4：规格与设计互相矛盾

**现象**：压缩测试的第二个断言始终失败——「裁剪后仍超阈值」。

**定位**：对比两份产物——

- `specs/test-suite/spec.md`：「断言提交给模型的请求长度**被压缩到阈值以内**」
- `design.md` 决策 7：「保留最近 `KEEP_RECENT_TURNS` 轮**原文**」

**根因**：两者在不寻常的输入下会矛盾——如果最近 K 轮本身就超阈值（例如某轮含一大段搜索结果），那么「保留原文」与「压缩到阈值内」无法同时成立。设计描述了一个**触发条件**，规格却把它当成了一个**硬保证**。

**处置**：规格是行为契约，优先满足它。实现上补一个硬上限 `CapTail`：保留最近 K 轮之后，若仍超阈值，就从最旧的一端**整轮**丢弃直到落回阈值内。唯一例外是最后一轮——即使它单独超阈值也保留，否则模型连当前的问题都看不到；这个例外不会失控，因为单条工具结果本身另有 2000 字符的截断上限。

同时回填 `design.md` 决策 7，把这条补进去。

**教训**：规格与设计冲突时，不要悄悄把规格改窄去迁就实现——那等于把缺陷写进文档。正确顺序是：确认哪一份是行为契约 → 满足它 → 回头修另一份。

---

### 问题 5：注册表被自己的别名卡住

**现象**：所有 Agent 层测试在装配阶段就失败——`工具 search 的别名 "websearch" 已被占用`。

**根因**：`search` 工具声明了别名 `web_search` 与 `websearch`。归一化（转小写、剥离 `_`）之后两者是**同一个键**，注册表检测到重复便拒绝了——**工具和自己冲突**。

**修复**：区分两种情况——

- **同一工具内部**归一化后重叠的别名（`get_weather` 与 `getWeather`）：无害，跳过即可
- **跨工具**的别名冲突：必须拒绝，否则模型请求的名称会指向错误的工具

**教训**：归一化会压缩命名空间，凡是基于归一化键做唯一性校验的地方，都要考虑「同一个实体的多个表示形式碰撞」这一情况。

---

### 问题 6：`-race` 在 Windows 上不可用

**现象**：`go test -race ./...` 报错退出。

**根因**：`go: -race requires cgo; enable cgo by setting CGO_ENABLED=1`——Windows 上的竞态检测器依赖 cgo，需要 C 工具链，而本机没有（与问题 1 同源）。

**处置**：这属于计划外的环境约束，影响了 `tasks.md` 中 7.5 与 9.6 两条任务的验证方式。**向需求方说明后由需求方决定**：确认不安装 C 编译器，改为以两项不变量断言替代——「交错耗时下回填顺序恒等于调用序号」与「该结论在重复 20 次运行中稳定」。`tasks.md` 的验证描述同步更新，README 中也如实说明了这条约束。

**教训**：验证方式本身也可能不可用。这时应当报告，而不是悄悄跳过验证或换一个较弱的检查还不说明。

---

### 问题 7：一次由工具链而非代码引起的假故障

**现象**：手工验证 CLI 时，第一条命令 `/help` 没有输出帮助文本，反而报了模型鉴权失败；`/new 查天气记待办` 的中文标题显示为 `??????`。

**定位**：改用在 UTF-8 环境下管道输入的方式复测，**全部命令正常，中文标题也正确显示**。

**根因**：这是 PowerShell 5.1 向原生程序传递管道输入时的编码行为所致，**不是程序缺陷**。进一步确认本机控制台代码页本就是 65001（UTF-8），交互式输入中文没有问题。

**教训**：验证工具本身也会产生假象。遇到可疑失败时，先用另一种方式复现一次，再决定是去改代码还是改验证手段。如果当时直接去「修」这个不存在的 bug，就会给程序加上一段无用的编码转换代码。

**同一类问题后来重演过一次**：加入配置文件功能后做冒烟测试，症状是「配置文件明明写了模型名，启动横幅显示的却是默认值」。看着像配置装载有 bug，实际是我把测试用的配置文件写到了临时目录，而默认位置是**进程的工作目录**——程序的行为完全正确，是验证方法错了。改用 `AGENT_CONFIG` 指定路径后一次通过。这类「验证方式本身出错」的情形在同一项目里出现了两次，值得记为一条稳定经验：**先确认是代码的问题还是验证的问题，再决定改什么。**

---

## 三、测试的实际作用

本项目的测试不只是回归防线，它在开发过程中直接抓出了上文的**问题 2、3、4、5** 四处真实缺陷。其中问题 2 尤其说明问题：那是一段「看起来更严谨」的检查代码引入的，人工审查几乎不会注意到，而测试一跑就现形。

因此测试的写法上有两条刻意的选择：

1. **注入传输层，而不是替换工具输出**。如果只把工具的输出做成可替换，测试就绕过了真实的解析逻辑——而问题 2 恰恰就出在解析层。注入传输层后，测试跑的是完整的真实解析路径，只是数据来自可控服务器。
2. **模拟模型做成可编排的脚本序列**。这样才能确定性地重现「请求工具 → 收到结果 → 再请求工具 → 给最终答复」这类多轮序列，以及参数跨分片到达、响应用于中途断开等边缘情况。固定桩做不到这些。

---

## 四、留给后续的判断

以下几点是当时判断可行但有意未做的，记录在此以免将来被当成疏漏：

- **工具结果的幂等复用**：若将来接入的工具有明显的重复执行成本，可以重新考虑。届时必须先区分纯函数工具与有状态工具，否则会引入正确性缺陷（见「关键的 Prompt 模式」第 4 条）。
- **流式响应的中途恢复**：当前中断后需重新提问。若体验上不可接受，可行的方向是保留已产出内容的长度，仅在其后追加而不是重试整个请求。
- **搜索的多供应商容错**：当前仅接博查。若要提高可用性，可在工具内部做供应商降级，这不需要改动循环或协议层。
