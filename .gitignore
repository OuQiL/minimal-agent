# 数据库与日志（含对话内容，不进版本库）
*.db
*.db-journal
*.db-wal
*.db-shm
*.log
logs/
data/

# 编译产物
/minimal-agent
/minimal-agent.exe
*.exe
*.test
*.out

# 密钥与本地配置
.env
.env.local
# 实际使用的配置文件可能含密钥，不进版本库；样例 config.example.yaml 会提交。
# 锚定到根目录——否则会连带排除 openspec/config.yaml，那是不含密钥的项目文件。
/config.yaml
/config.local.yaml

# 编辑器
.idea/
.vscode/
*.swp
/需求
